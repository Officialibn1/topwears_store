import React from "react";
import { Button } from "./button";

type Props = {};

const CheckOutButton = (props: Props) => {
	return <Button variant={"secondary"}>Checkout Now</Button>;
};

export default CheckOutButton;
